# CURRENT_TASK

**Phase 2 complete** (design system: tokens, fonts, motion, components, redesigned homepage; build + check pass). Do not redo: scaffold, tokens, layout, views, routes, SEO framework, Phase 2 components.

**Next: Phase 3 — Core page hardening.** Internal-link graph per brief §58, BreadcrumbList JSON-LD, breadcrumb UI on remaining detail pages, service-page enrichment (symptoms/process sections from verified capability list only), footer services list link check, then Phase 4 (3D island + primitive-built placeholder forklift).

Build debt: `astro check` wired with 0 errors; lint (eslint) still deferred to Phase 9.
