---
import BaseLayout from '../layouts/BaseLayout.astro';
import { site } from '../config/site';
import type { Locale } from '../i18n/ui';
import { faqs } from '../content/misc';

const { locale = 'en' as Locale } = Astro.props;
---

<BaseLayout locale={locale} path="/faq/" title={`FAQ — ${site.name}`} description="Frequently asked questions about NEEL ENTERPRISES forklift repair, maintenance, batteries, spare parts, rental and coverage.">
  <section class="section-pad">
    <div class="container-neel max-w-3xl">
      <p class="label-mono">FAQ</p>
      <h1 class="mt-4 text-4xl font-black tracking-tight md:text-5xl">Questions, answered.</h1>
      <div class="mt-10 divide-y divide-line rounded-sm border border-line bg-elevated">
        {faqs.map((f) => (
          <details class="faq p-5" data-reveal>
            <summary class="font-semibold">{f.q}</summary>
            <p class="mt-3 text-muted">{f.a}</p>
          </details>
        ))}
      </div>
      <div class="mt-10 rounded-sm border border-line bg-elevated p-6" data-reveal>
        <p class="font-semibold">Didn't find your answer?</p>
        <p class="mt-1 text-sm text-muted">Ask directly — we respond to real enquiries from real equipment.</p>
        <div class="mt-4 flex flex-wrap gap-3">
          <a href={`/${locale === 'en' ? '' : locale + '/'}request-service/`} class="btn btn-primary">Request Service</a>
          <a href={`/${locale === 'en' ? '' : locale + '/'}services/`} class="btn btn-quiet">Explore Services</a>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<script type="application/ld+json" set:html={JSON.stringify({
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((f) => ({
    '@type': 'Question',
    name: f.q,
    acceptedAnswer: { '@type': 'Answer', text: f.a },
  })),
})} />
